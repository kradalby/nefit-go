use flake

# Nefit Easy credentials via setec (resolved in parallel)
secret_env <<'SECRETS'
  NEFIT_SERIAL_NUMBER  infra/ldn/nefit/serial-number
  NEFIT_ACCESS_KEY     infra/ldn/nefit/access-key
  NEFIT_PASSWORD       infra/ldn/nefit/password
SECRETS

[ -f .envrc.local ] && source .envrc.local
